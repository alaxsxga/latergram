# Cloudflare 部署設定：驗證中間頁（靜態頁，無 Worker 程式碼）
# 對應 Dashboard 上以「Upload static files」建立的 latergram-verify。
# 部署指令：npx wrangler deploy（只上傳 ./web，不含 repo 其他內容）
name = "latergram-verify"
compatibility_date = "2025-11-01"

[assets]
directory = "./web"
